---
title: 문서검증 확인목록
tags:
  - dabitdocs
  - 검수
last_updated: 2026-08-11
---

# 문서검증 확인목록 (2026-08-11 문서 전수 수정 후)

문서 90개를 DabitOne v4.3.0 코드와 대조해 44개를 수정했다.
아래는 **코드만으로 결론 못 내려서, 실기 테스트나 사람 판단이 필요한 항목**이다.

## A. 실기 테스트 필요 (전광판·컨버터 연결)

- [ ] **A1. UDP 포트 5107 vs 5108** — 가장 중요
	- 문서(2.1.4): "Ethernet은 5108, wifi는 5107"
	- 앱: `5108` 하드코딩 + 읽기전용이라 5107 입력 자체가 불가
	- 테스트: WiFi 컨버터(DB300WB)를 UDP로 연결해 5108로 통신되는지 확인
	- 결과에 따라: 5107이 맞으면 → **앱 수정 이슈 등록**, 5108로 되면 → **문서에서 5107 문구 삭제**
- [ ] **A2. 정보문구 X종료/Y종료 경계값** (3.27)
	- 예시 표의 종료값(96×32 기준 X 0→96, Y 0→16 등)은 추정치
	- 테스트: 정보문구설정 창에서 실제 경계값이 포함인지 제외인지 확인 후 예시 보정
- [ ] **A3. 색상모드 전환 후 표출** (7.12)
	- 08C↔08B 전환 후 재시작·색 표현이 정상인지. 보드·펌웨어별 동작 차이 있음 (문서에도 "현장 검증 필요" 명시해 둠)
- [ ] **A4. 콘텐츠 색심도 16비트(RGB565)** — 신규 문서 작성 전제조건
	- v4.3.0 기능이지만 실보드 표출 검증 대기 상태. 16C 보드에서 그라데이션 이미지 표출 확인 후 문서화

## B. BLE 문서 2개 — 실기 확인 후 재작성 (이번에 제외한 것)

- [ ] **B1. 2.1.5 BLE 통신 설정** — 절차 전체가 레거시(DBPS) 기준
	- 없는 버튼: `포트열기`/`포트닫기`/`컨트롤러 연결`
	- 실제: `통신` 탭 `BLE` 버튼 → `BLE 설정` 창 (`통신포트`+`포트검색`, `연결`/`연결끊기`, `불러오기`/`변경`)
	- 테스트: WifiBLE 컨버터로 연결→전송까지 실제 흐름 확인 후 재작성
- [ ] **B2. 3.22 BLE 이름·비밀번호 변경**
	- 문서의 `검색`→실제 `불러오기`, `전송`→실제 `변경`
	- ⚠️ 이 파일에는 이전 세션의 미커밋 수정(+21줄)이 이미 있음 — 내용 먼저 확인

## 🚨 E. 자동배포 워처 — 재발 방지 결정 필요 (2026-08-11 사고)

- [x] **E1. 자동배포 중단 — (a)안 채택, 스크립트 차단 완료 (2026-08-11)**
	- `auto-deploy.ps1` 첫머리에서 로그만 남기고 즉시 종료하도록 막음. 실행 테스트로 0.7초 종료·배포 없음 확인
	- ⬜ **남은 한 단계**: 예약작업 자체 비활성화는 `RunLevel=Highest`라 **관리자 권한 필요**. 관리자 PowerShell에서 아래 한 줄 실행
	  ```
	  Disable-ScheduledTask -TaskName 'DabitDocs-AutoDeploy'
	  ```
	- 안 해도 스크립트가 막혀 있어 피해는 없음. 로그온 때마다 즉시 종료되는 프로세스가 하나 뜰 뿐
	- 배포는 `/deploy` 스킬로 수동 실행
	- 사고: Synology Drive 동기화 도중 파일이 잠깐 사라진 상태를 워처의 `quartz sync`가 `git add -A`로 스냅샷 커밋 → **content 92개 → 25개로 삭제된 채 GitHub에 push** → docs.dabitsol.com 3~8장 전부 404
	- 복구: 마지막 정상 트리(5292f5b) content 복구 후 재배포 완료. git 객체가 남아 있어 실제 데이터 손실은 없었음
	- 근본 원인: **Synology 동기화 폴더 안의 git 저장소를 `git add -A`로 자동 커밋·push**하는 구조. 다른 PC(DT-JYHEO)도 같은 폴더를 동기화 중이라 충돌 파일(`_DT-JYHEO_..._Conflict.md`)까지 생김
	- 선택지: (a) 워처 영구 비활성 + 배포는 수동 `/deploy`만 ← 추천 / (b) 워처를 `git add content/`로 제한하고 삭제 감지 시 중단하는 가드 추가 / (c) 저장소를 Synology 동기화 밖 경로로 이전
- [ ] **E2. 다른 PC(DT-JYHEO)에서 같은 폴더 동기화 여부 확인** — 충돌 파일이 생긴 원인. CTO와 조율 필요

## C. 렌더링·발행 확인 (PC에서 바로 가능)

- [ ] **C1. 표 구조 바뀐 3개 문서** — Obsidian 미리보기나 Quartz 빌드로 확인
	- 3.23 효과 코드표 (HEX 열 전면 교체 + 3D효과 절 신설)
	- 3.27 정보문구 (위치 입력 4칸 재구성)
	- 2.6.2 LED 모듈 배열 (마지막 표 세로|가로 열 순서 변경)
- [ ] **C2. 신기능_UI경로_검수목록.md 미체크 7건** 처리
- [ ] **C3. 7장 캡처 플레이스홀더 채우기** — 〔캡처 1장〕 표시 남은 곳: 7.1(2곳)·7.2·7.3·7.6·7.7·7.8·7.11·7.12 (2026-08-11 재번호 기준)
	- 특히 7.11 색상모드는 UI가 바뀌어(고급 탭 본문 이동) 기존 캡처 재사용 불가
- [x] **C7. AutoSend 문서 제외 (2026-08-11)** — 사내 전용 프로그램이라 공개 문서에서 삭제. 7.2 자리가 비면 눈에 띄므로 뒤 번호를 당겨 7.1~7.12 연속으로 재정렬(링크 10개 파일 갱신). 챗봇 인덱스에서도 제거
- [x] **C5. 배포 완료 (2026-08-11)** — 사이트·챗봇 양쪽 반영. 챗봇 실측 15문항 결과는 [[챗봇검증_QA]]
- [ ] **C6. 2.7.4(컨트롤러에 업로드된 표시목록·이미지 확인) 처리 결정** — v4에만 있던 다빛채 전용 문서가 병합에서 살아남아 구 "(다빛채)" 폴더에 홀로 남음. 삭제 vs DabitOne(콘텐츠 관리 창) 기준 재작성
- [x] **C4. 커밋·배포 완료 (2026-08-11)** — 이번 세션분(3ad231e)·이전 세션분(cbc6587) 분리 커밋 → v4 병합(983adec) → GitHub push·사이트 배포. v4에 있던 미커밋 초안(다빛채 2.7 수정·3.12 DS1302 병기)은 stash로 보존: `git stash list` 참고

## D. 결정 대기 (사람 판단)

- [ ] **D1. 신규 문서 [상] 4건 작성 여부** — 7장에 추가할 미문서화 기능
	1. 다크 모드 5종 테마 (가장 쉽고 임팩트 큼)
	2. 표출신호 자동 탐색 (현장 실용성 1위)
	3. 이미지 편집 도구 강화 (매뉴얼 2.7.1은 이미 갱신함 — 신기능 문서로 별도 승격할지)
	4. 콘텐츠 색심도 16비트 (A4 검증 후)
- [ ] **D2. DabitOne 코드 결함 의심 1건** — 별도 작업 칩 등록됨
	- `ScheduleTransferService.ValidateFileSizes`가 파일명만 저장된 항목의 크기 검증을 조용히 건너뜀 (전송 차단 가드 무력화 경로)
- [x] **D4. DB520·720 기능별 지원 여부 — 이부장 확인 완료 (2026-08-11)**
	- [x] 3.12 RTC — 모든 보드 동일하게 DS3231 외장 부착 → 문서에 DB520·720 추가 완료
	- [x] 3.13 GPS — 별도 GPS 수신기 연결 방식 동일 → 문서에 DB520·720 추가 완료
	- [x] 2.1.3 RS485 — 별도 RS485 보드 부착 + 딥스위치 방식 (DB402만 내장) → 문서에 한 줄 추가 완료
	- [x] 3.19 무선시험 / 3.29 32duty / 2.6.2 배열 / 3.7 신호분배보드 / 3.27 정보문구 — **건드리지 않기로 확정**
- [ ] **D3. DB520·720 후속 문서 작성 여부**
	- 웹서버·웹 UI 사용자 안내 문서 (RnD 정본: `00_Dabit/10_RnD/40_Project/DIBD_DB720_DB520_웹서버_구축_적재_가이드_다빛원_2026-07-06.md` 기반으로 사용자용 요약)
	- DCV2 보드 콘텐츠 관리 창(DB520/720 전용)은 **출시 게이트 미해제** — 펌웨어 S9-b 저장손상 fix 반영 전 문서화 금지
	- 소개 문서 DB600 표기를 "112개 (예: 4단 28열)"로 고침 — DB600V 기준. 예시를 DB600T(80개, 4단 20열)로 갈지 CTO 확인

## 신제품 반영 완료 (2026-08-11)

- [[content/3. 전문자료/3.2. 컨트롤러별 최대 표출 가능 모듈|3.2]] — DB520(S·T)·DB720(S·T·U) 색상 모드별 최대 표출 표 추가 (정본: `21_Project/PRJ260706-21-신제품런칭페이지/02_표출크기_펌웨어정리.md`)
- 다빛솔루션 소개 — 라인업 표에 DB520·DB720 행 추가 (웹서버 내장, 16비트는 DB720 전용)
- 견적 문의 안내 — 견적 품목에 DB520·DB720 추가
- 가격은 문서 사이트에 미기재 (견적 문의 유도 유지). WiFi/BLE는 인증 대기라 표기 안 함

## 완료된 것 (참고)

- 대표번호 2435로 통일 (견적 문의 2436 → 2435) ✅
- 7장 9개 + 1~2장 14개 + 3장 18개 + 4~5장 3건 수정 ✅
- 레거시(다빛채·시뮬레이터) 절 라벨은 옛 표기 보존 ✅

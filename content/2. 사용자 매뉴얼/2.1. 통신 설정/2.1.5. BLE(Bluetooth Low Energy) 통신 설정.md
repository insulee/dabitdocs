
> [!abstract] LAN to Serial 컨버터 BLE버전의 통신 설정 방법
> LAN to Serial 컨버터인 DB300WB를 통해 전광판 컨트롤러를 BLE 통신으로 연결하는 방법에 대한 가이드

#### 연결 방식 및 기본값
- DB300WB --(RS232/J2)-- DB500/600 --(J1)-- 5V전원
- 기본값
	- RS232로 컨트롤러와 연결, TTL로 변경 가능 - [[3.5. DB300 탭 항목 설명]]
	- 이름: `dabit_ble`, 비밀번호: `dabit_ble`
	- 이름 및 비밀번호 변경은 노트북 또는 PC에서 다빛솔루션 소프트웨어를 통해서만 가능
		- [[3.22. WifiBLE to Serial 컨버터 BLE 이름 및 비밀번호 변경]]
	- DB300WB가 Client TCP/IP 모드에서만 정상적으로 연결

#### 노트북/PC로 연결
- 윈도우 OS
	1. Bluetooth 및 장치 > `dabit_ble`(초기값) 선택 > 페어링됨 확인
	2. 장치 관리자 > 포트(COM & LPT) > 1~2개의 Bluetooth 포트 확인
- 다빛솔루션 소프트웨어
	1. `통신 설정` > 통신 설정 창
		- `Serial` 선택
		- 통신 포트 설정: `Bluetooth 포트`
		- 통신 속도 설정: `115200`
		- `포트열기`
		- `포트닫기`
	2. `Bluetooth/dbBle` > 블루투스 설정 창
		- 블루투스 이름: `dabit_ble`
		- 블루투스 비밀번호: `dabit_ble`
		- `BeginBLE/시작`
		- 소프트웨어 하단의 로그 보기 > `![DIBD BLE OK!]` 확인
		- `닫기`
	3. `포트열기`
	4. `컨트롤러 연결`

#### 다빛채 어플로 연결

1. 스마트폰(안드로이드)에 DabitChe_App을 다운로드하여 설치
2. 블루투스를 켜고 기기를 검색
3. `dabit_ble`(초기값) 선택 후 연결
	- 동일한 이름의 블루투스 기기가 이미 등록된 경우 장치를 제거
4. 다빛채 어플 실행
	- `블루투스(BLE)` 선택
	- 암호입력: `dabit_ble` (기본값)
	- `연결하기`
	- 블루투스 이름(`dabit_ble`) 선택
5. "전광판에 블루투스(BLE)로 연결되었습니다."라는 문구가 표시되면 연결 완료
	- 사용하지 않고 5분이 경과하면 연결 종료
	- 재연결 하려면, `연결끊기` >  `연결하기`
	- 기존 사용자가 앱에서 `연결끊기`해야 다른 스마트폰에서 연결 가능
6. 참고
	- 다빛채 어플 > 패킷직접입력 창을 통해서 아스키 프로토콜 형태의 패킷 전송이 가능
	- 한글을 표출하려면, `/F0203` 패킷을 삽입하여 UTF8로 인코딩해서 전송 (16x16 픽셀 기준)



**아래 영상을 통해 내용을 더 쉽게 이해할 수 있습니다.**

<iframe width="560" height="315" src="https://www.youtube.com/embed/Tv0w9qJk7-4?si=e2kj5RbrUZwPhlr8" title="YouTube video player" frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share" referrerpolicy="strict-origin-when-cross-origin" allowfullscreen></iframe>

다빛솔루션 유튜브 채널에서 보기: https://www.youtube.com/watch?v=Tv0w9qJk7-4&t=3s



---

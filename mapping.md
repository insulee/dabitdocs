# Phase 2 매핑 — DabitOne 대응 표현

**범위**: `1. 시작하기/`, `2. 사용자 매뉴얼/`(2.10 제외), `3. 전문자료/`
**제외**: 2.10 DBPS / 5 다운로드 / 6 연혁 / 다빛채 어플(모바일) / 외부 PDF 링크 / 2.7.x 폴더 이름

**DabitOne UI 메뉴 구조**: 좌측 사이드바 5탭 = `통신` / `설정` / `전송` / `편집` / `고급`

**버튼 처리 규칙**:
- 인라인 GroupBox(`설정` 탭의 표출신호/전광판 화면 구성/폰트전송 등): `설정` 버튼 → `전송`, `닫기` 삭제
- 별도 다이얼로그 창(`고급` 탭의 BgSchedule/Relay/FontWeight/InfoText 등): `전송`/`저장`/`적용` + `닫기`

---

## A. 패턴 A — DBPS / 다빛채 두 섹션 통합

### A-1. `2. 사용자 매뉴얼/2.2. 전광판 크기 설정/2.2.1. 전광판 크기 설정.md`

**라인 2 (Pattern C)**:
```diff
->[!abstract] 다빛 프로토콜 시뮬레이터와 다빛채에서 전광판 크기 설정 방법 안내
+>[!abstract] DabitOne에서 전광판 크기 설정 방법 안내
```

**라인 4-9 (Pattern A 통합)**:
```diff
-### 전광판 크기 설정 경로
-
-#### DBPS
-- `설정` > 전광판 화면 구성
-#### 다빛채
-- `환경설정` > `화면설정`
+### 전광판 크기 설정 경로
+
+#### DabitOne
+- `설정` > `전광판 화면 구성`
```

**라인 17 (Pattern B 버튼명 변경)**:
```diff
-- `설정`
+- `전송`
```
근거: MainWindow.xaml:438-519 (GroupBox Header="전광판 화면 구성", Button "전송")

---

### A-2. `2. 사용자 매뉴얼/2.3. 표출신호 설정/2.3.1. 표출신호 설정.md`

**라인 4-9 (Pattern A 통합)**:
```diff
-### 표출신호 설정 경로
-
-#### DBPS
-- `설정` > `표출신호 설정`
-
-#### 다빛채
-- `환경설정` > `표출신호 설정`
+### 표출신호 설정 경로
+
+#### DabitOne
+- `설정` > `표출신호 설정`
```

**라인 16-17 (Pattern B 버튼명 변경 + 라인 삭제)**:
```diff
-- `설정`
-- `닫기`
+- `전송`
```
근거: MainWindow.xaml:522-574 (인라인 GroupBox Header="표출신호 설정", Button "전송"; "닫기" 없음)

---

### A-3. `2. 사용자 매뉴얼/2.6. LED 모듈/2.6.2. LED 모듈 배열방법.md`

**라인 5-9 (Pattern A 통합)**:
```diff
-### 설정 경로
-#### DBPS
-- `설정` > 전광판 화면 구성 > `모듈배열방법`
-#### 다빛채:
-- `환경설정` > `화면설정` > 전광판 화면 설정 창 > `모듈배열방법`
+### 설정 경로
+#### DabitOne
+- `설정` > `전광판 화면 구성` > `배열`
```
근거: MainWindow.xaml:504-517 (ComboBox Items="가로형"/"1줄 세로형"/"2줄 세로형" 등, "배열" 라벨)

> ⚠️ 본문 line 12+ 의 예시들엔 `모듈배열방법: 가로형(default)` 같은 표현이 다수. 이건 **본문 변수 라벨이라 유지** (UI에선 ComboBox 라벨이 "배열"이지만, 매뉴얼 본문에서 "모듈배열방법"으로 설명하는 건 무방).

---

### A-4. `3. 전문자료/3.21. 컨트롤러 펌웨어 정보 해석.md`

**라인 7-11 (Pattern A 통합 — 번호 리스트 형태)**:
```diff
 - 아래 방법 중 하나를 사용하여 펌웨어 정보를 확인
-	1. 다빛채
-	    - 다빛채 프로그램 > 메뉴에서 `정보 > 버전 읽기`
-	2. DBPS
-	    - DBPS > `설정 > 펌웨어 업그레이드 > 펌웨어 읽기`
-	3. ASCII 또는 헥사 프로토콜 전송
+	1. DabitOne
+	    - `고급` > `펌웨어 업데이트` > `펌웨어 읽기`
+	2. ASCII 또는 헥사 프로토콜 전송
 	    - ASCII 프로토콜: `![0081!]`
 	    - 헥사 코드: `10 02 00 00 02 6F F1 10 03`
```
근거: MainWindow.xaml:1220-1244 (GroupBox Header="펌웨어 업데이트", Button "펌웨어 읽기")

> ⚠️ 구 다빛채의 `정보 > 버전 읽기` 메뉴와 DBPS의 `설정 > 펌웨어 업그레이드 > 펌웨어 읽기`가 통합된 DabitOne 경로는 `고급 > 펌웨어 업데이트 > 펌웨어 읽기`로 매핑. 별도 "정보 메뉴"는 DabitOne에 없음 (Agent 분석 기준).

---

### A-5. `3. 전문자료/3.22. WifiBLE to Serial 컨버터 BLE 이름 및 비밀번호 변경.md`

**라인 18-38 (Pattern A 통합 — 두 큰 블록)**:
```diff
 ### 소프트웨어 설정
-#### DBPS
-- `통신설정`
-	- `Serial` 선택
-	- 통신 포트 설정: `컨버터가 연결된 포트` 
-	- 통신 속도 설정: `115200bps`(기본값)
-	- `dbBle` 
-		- 통신 포트 설정: `컨버터가 연결된 포트` 
-		- `검색` > 블루투스 이름(dabit_ble) 및 블루투스 비밀번호(dabit_ble) 조회
-		- 변경할 이름과 비밀번호 입력 후 `설정`
-		- 소프트웨어를 종료 후 `통신설정` > `dbBle` > `검색` > 변경되었는지 확인
-
-#### 다빛 프로토콜 시뮬레이터 / 다빛채
-- `통신설정` 
-	- `Serial` 선택
-	- 통신 포트 설정: `컨버터가 연결된 포트` 
-	- 통신 속도 설정: `115200bps`(기본값)
-- `Bluetooth`
-	- `Search` > 기본 이름(dabit_ble) 및 비밀번호(dabit_ble) 조회
-	- 변경할 이름과 비밀번호 입력 > `Set`
-	- 소프트웨어를 종료 후 `통신설정` > `Bluetooth` > `Search` > 변경되었는지 확인
+#### DabitOne
+- `통신` > `통신 설정`
+	- `Serial` 선택
+	- 통신 포트 설정: `컨버터가 연결된 포트`
+	- 통신 속도 설정: `115200bps`(기본값)
+- `통신` > `BLE`
+	- `검색` > 기본 이름(dabit_ble) 및 비밀번호(dabit_ble) 조회
+	- 변경할 이름과 비밀번호 입력 > `전송`
+	- 소프트웨어를 종료 후 `통신` > `BLE` > `검색` > 변경되었는지 확인
+	- `저장` 후 `닫기`
```
근거: MainWindow.xaml:91-414 (통신 설정 영역), BleSettingWindow.xaml:1-42 (Title="BLE 설정", Button "검색"/"전송"/"저장"/"닫기")

> ⚠️ DabitOne의 BLE 다이얼로그는 별도 창(BleSettingWindow). "BLE Name", "BLE Password" 텍스트박스 + "검색"/"전송"/"저장"/"닫기" 버튼. 본문은 단순화 통합.

---

## B. 패턴 B-1 — DBPS만 등장 (메뉴 단계 본문 안내)

### B-1-1. `2. 사용자 매뉴얼/2.1. 통신 설정/2.1.3. RS485 통신 설정.md`

**라인 14**:
```diff
-	- DBPS> `통신 설정` > Serial > RS-485 Address : `DABIT 01`
+	- DabitOne > `고급` > `보드기능설정` > `RS485 Address` : `DABIT 01`
```
근거: MainWindow.xaml:1133-1214 (GroupBox Header="보드기능설정", "RS485 Address" 라벨). 통신 설정 영역에는 RS485 Address가 없고, 보드기능설정에 있음.

> ⚠️ 구 DBPS의 `통신 설정 > Serial > RS-485 Address`가 DabitOne에선 보드기능설정으로 이동된 것으로 보임. **확인 필요**.

---

### B-1-2. `2. 사용자 매뉴얼/2.4. 폰트 설정/2.4.1. 폰트 변경.md`

**라인 6**:
```diff
-- DBPS> `설정` > `폰트 전송`
+- DabitOne > `설정` > `폰트전송`
```
근거: MainWindow.xaml:579-626 (GroupBox Header="폰트전송" — 띄어쓰기 없음 주의)

**라인 29 (모바일 어플 — 유지)**: `다빛채 어플에서 전송시` → 그대로 유지

---

### B-1-3. `2. 사용자 매뉴얼/2.4. 폰트 설정/2.4.4. 세로형 폰트 설정 방법.md`

**라인 10**:
```diff
-1. DBPS > 설정 > 폰트 설정
+1. DabitOne > `설정` > `폰트전송`
```

**라인 19**:
```diff
-1. DBPS > 프로토콜 전송 > `아스키 프로토콜`
+1. DabitOne > `전송` > `메시지 전송` > `ASCII 프로토콜`
```

**라인 26 (Pattern C 참고1)**:
```diff
-#### (참고1) 다빛프로토콜시뮬레이터, 다빛채에서 폰트 전송시
+#### (참고1) DabitOne에서 폰트 전송시
```

**라인 37**:
```diff
-- DBPS > 설정 > 폰트설정 > 폰트그룹 2 체크 > 아래의 폰트파일 선택 및 전송
+- DabitOne > `설정` > `폰트전송` > 폰트그룹 2 체크 > 아래의 폰트파일 선택 및 `폰트파일 전송`
```

**라인 41**:
```diff
-- DBPS > 프로토콜 전송
+- DabitOne > `전송` > `메시지 전송`
```

**라인 47 (모바일 어플 — 유지)**: `(참고2) 다빛채 어플에서 전송시` → 그대로 유지
근거: MainWindow.xaml:579-626 (폰트전송 GroupBox), MainWindow.xaml:633-730 (메시지 전송 영역)

---

### B-1-4. `2. 사용자 매뉴얼/2.4. 폰트 설정/2.4.5. 16x16 폰트(영문,숫자) 설정 방법.md`

`2.4.4.`와 동일 패턴. 라인 10/19/26/37/41 모두 동일 변환 적용. 라인 47 모바일 어플은 유지.

---

### B-1-5. `2. 사용자 매뉴얼/2.5. 메시지 종류 및 섹션기능/2.5.1. 메시지 종류.md`

**라인 59**:
```diff
-#### DBPS에서 전송 방법
+#### DabitOne에서 전송 방법
```
근거: 메뉴 경로 변환이 아닌 헤더 단어 치환만.

---

### B-1-6. `3. 전문자료/3.11. 외부장치 제어용 릴레이 연결 및 설정.md`

**라인 23**:
```diff
-#### 연결 후 테스트 방법 (DBPS)
+#### 연결 후 테스트 방법 (DabitOne)
```
근거: 헤더 단어 치환. 본문은 별도 검토 필요.

> ⚠️ 본문에 어떤 메뉴 경로가 있을지 미정. 실제 수정 시 파일 전체 재확인.

---

### B-1-7. `3. 전문자료/3.12. RTC 모듈(DS3231)을 이용한 시간 유지 방법.md`

**라인 16**:
```diff
-- DBPS > 설정 > `펌웨어 정보 읽기`에서 버전 확인 가능
+- DabitOne > `고급` > `펌웨어 업데이트` > `펌웨어 읽기`에서 버전 확인 가능
```

**라인 33**:
```diff
-1. DBPS > 설정 > `보드 기능 설정` > `읽기`
+1. DabitOne > `고급` > `보드기능설정` > `읽기`
```

**라인 39**:
```diff
-1. **최초 시간 설정**: DBPS > 설정 > `PC 시간 동기화`
+1. **최초 시간 설정**: DabitOne > `고급` > `시간설정` > `PC 시간 전송`
```

**라인 40**:
```diff
-2. **시간 확인**: DBPS > 설정 > `컨트롤러 시간읽기` 또는 [[2.8.1. ASCII 프로토콜 매뉴얼|ASCII 프로토콜]] `![0031!]`을 전송하여 응답으로 확인
+2. **시간 확인**: DabitOne > `고급` > `시간설정` > `컨트롤러 시간 읽기` 또는 [[2.8.1. ASCII 프로토콜 매뉴얼|ASCII 프로토콜]] `![0031!]`을 전송하여 응답으로 확인
```
근거: MainWindow.xaml:1121-1130 (GroupBox Header="시간설정", Button "PC 시간 전송", "컨트롤러 시간 읽기")

---

### B-1-8. `3. 전문자료/3.13. GPS 수신기 설정 및 테스트.md`

**라인 9**:
```diff
-- DBPS의 보드기능 설정에서 RS232 또는 TTL의 Baud Rate를 9600으로 설정
+- DabitOne의 `고급` > `보드기능설정`에서 J2(RS232) Baud 또는 J3(TTL) Baud를 9600으로 설정
```

**라인 27**:
```diff
-- DBPS > 설정 > `컨트롤러 시간읽기`: 현재 시간이 정상적으로 나오면 GPS 정상 연결
+- DabitOne > `고급` > `시간설정` > `컨트롤러 시간 읽기`: 현재 시간이 정상적으로 나오면 GPS 정상 연결
```
근거: MainWindow.xaml:1171-1193 (J2/J3 Baud ComboBox)

---

### B-1-9. `3. 전문자료/3.24. 폰트 두께 계수 설정.md`

**라인 4**:
```diff
-> - DBPS > 설정 > 추가 기능 설정에서 폰트 두께 계수를 설정
+> - DabitOne > `고급` > `폰트 가중치` 다이얼로그에서 Width / Height를 설정
```
근거: FontWeightWindow.xaml:1-19 (Title="폰트 가중치", Width/Height ComboBox, "전송"/"닫기")

> ⚠️ DabitOne에선 "폰트 두께 계수"가 "폰트 가중치"라는 다이얼로그로 분리됨. 본문 단어도 "폰트 두께 계수" → "폰트 가중치"로 변경할지, 매뉴얼 제목과 본문은 두께 계수 그대로 유지할지 결정 필요.

---

### B-1-10. `3. 전문자료/3.3. 배경이미지 제작 및 표출.md`

**라인 6 (Pattern C)**:
```diff
-2. [[2.7.2. 이미지 또는 문자를 dat 또는 ani 파일로 변환]]: 다빛채에 이미지를 불러오기 위해 .dat 또는 .ani 파일로 변환
+2. [[2.7.2. 이미지 또는 문자를 dat 또는 ani 파일로 변환]]: DabitOne에 이미지를 불러오기 위해 .dat 또는 .ani 파일로 변환
```

**라인 11 (Pattern C)**:
```diff
-	 - DBPS 또는 다빛채에서 다음의 프로토콜로 표출
+	 - DabitOne에서 다음의 프로토콜로 표출
```

**라인 15-16 (Pattern A 통합)**:
```diff
-	- DBPS > `설정` > 배경이미지 > `1번 이미지` > `전송`
-	- 다빛채 > `고급설정` > `Protocol Simulator(V6)` > `특수기능` > 배경이미지 선택 > `1번` > `전송`
+	- DabitOne > `고급` > `배경 스케줄` > 1번 이미지 선택 > `전송`
```

**라인 19**:
```diff
-- 다빛채 v2.3.0 이상 또는 DBPS에서 가능
+- DabitOne에서 가능
```

**라인 23**:
```diff
-- DBPS > 설정 > `추가 기능 설정` > `배경이미지 스케줄` > 항목 선택 > `전송`
+- DabitOne > `고급` > `배경 스케줄` > 항목 선택 > `전송`
```
근거: BgScheduleWindow.xaml:1-28 (Title="배경 스케줄", "선택된 이미지" 체크박스 리스트 최대 10개, "전송"/"닫기")

> ⚠️ 단독 1번 이미지 전송 vs 스케줄 전송이 DabitOne에서 같은 다이얼로그(배경 스케줄)인지 별도인지 확인 필요. Agent 보고는 `배경 스케줄`만 언급.

---

## B-2. 패턴 B-2 — 다빛채만 등장

### B-2-1. `2. 사용자 매뉴얼/2.9. 펌웨어, 보드기능설정/2.9.1. 펌웨어정보읽기 및 업로드.md`

**라인 36 (헤더)**:
```diff
-### 다빛채
+### DabitOne
```
근거: 단순 단어 치환. 하위 본문은 별도 검토.

> ⚠️ 파일 전체를 보고 메뉴 경로 변경 여부 확인 필요.

---

### B-2-2. `3. 전문자료/3.10. 조도센서 연결 및 설정방법.md`

**라인 13**:
```diff
-#### 다빛채 설정
+#### DabitOne 설정
```

> ⚠️ 본문 검토 후 메뉴 경로 (조도/CDS 연동) 변환 필요. 후보: BrightnessAdvancedWindow.xaml의 "휘도 고급설정" > "CDS연동".

---

### B-2-3. `3. 전문자료/3.27. 정보문구 표시 방법.md`

**라인 3 (Pattern C abstract)**:
```diff
-> ASCII 프로토콜과 다빛채에서 정보문구 표시하는 방법 안내
+> ASCII 프로토콜과 DabitOne에서 정보문구 표시하는 방법 안내
```

**라인 10 (헤더)**:
```diff
-#### 다빛채 정보문구 설정
+#### DabitOne 정보문구 설정
```

**라인 11-12 (본문 메뉴 경로)**:
```diff
-- 정확한 날짜와 시간을 표시하기 위해 컨트롤러 접속 > PC시간으로 동기화 
-- 표시파일작성 > 정보문구설정
+- 정확한 날짜와 시간을 표시하기 위해 `고급` > `시간설정` > `PC 시간 전송`
+- `고급` > `정보문구설정`
```

**라인 13**:
```diff
-	- 다빛채 정보문구 설정창![[정보문구1.png]]
+	- DabitOne 정보문구설정 창![[정보문구1.png]]
```

**라인 23 (헤더)**:
```diff
-#### 프로토콜 및 다빛채 코드표
+#### 프로토콜 및 DabitOne 코드표
```

**라인 25 (표 헤더)**:
```diff
-| 구분     | 표시 내용 ... | **ASCII 프로토콜<br>/ixx** | **다빛채<br>코드표** | 표시 예       |
+| 구분     | 표시 내용 ... | **ASCII 프로토콜<br>/ixx** | **DabitOne<br>코드표** | 표시 예       |
```

**라인 85-86**:
```diff
-2. 다빛채 정보문구설정
-	- 다빛채에서는 한 섹션당 최대 6개의 정보 입력 가능
+2. DabitOne 정보문구설정
+	- DabitOne에서는 한 섹션당 최대 6개의 정보 입력 가능
```
근거: InfoTextSettingsWindow.xaml:1-349 (Title="정보문구설정", "Clear"/"초기값 Load"/"전송"/"닫기")

---

### B-2-4. `3. 전문자료/3.30. 페이지 문구를 배경이미지 번호로 표시.md`

**라인 7**:
```diff
-1. 다빛채 실행 및 통신 연결
+1. DabitOne 실행 및 통신 연결
```

**라인 12**:
```diff
-	- 다빛채 메인 화면에서 일반문구 5개 등록
+	- DabitOne 메인 화면에서 일반문구 5개 등록
```

**라인 20 (Pattern C, 3제품 동시언급)**:
```diff
-- dbPS, DABIT Protocol Simulator, 다빛채 에서 페이지 문구 패킷 전송
+- DabitOne에서 페이지 문구 패킷 전송
```

**라인 35**:
```diff
-	- 다빛채 메인 화면에서 표시파일에 공백화면 1개만 등록
+	- DabitOne 메인 화면에서 표시파일에 공백화면 1개만 등록
```

---

## C. 패턴 C — 서술형 / 우회 표현

### C-1. `1. 시작하기/제품 시작 가이드.md`

**라인 3**:
```diff
-> 다빛솔루션의 전광판 컨트롤러와 LED 모듈 연결에 관한 가장 기초적인 가이드로, 최적의 학습효과 달성을 위해 실제 장비를 연결하고 가이드의 각 단계를 **DBPS**로 따라하기를 권장합니다.
+> 다빛솔루션의 전광판 컨트롤러와 LED 모듈 연결에 관한 가장 기초적인 가이드로, 최적의 학습효과 달성을 위해 실제 장비를 연결하고 가이드의 각 단계를 **DabitOne**으로 따라하기를 권장합니다.
```

**라인 10**:
```diff
-- [[소프트웨어 다운로드]]에서 최신 버전의 DBPS 다운로드
+- [[소프트웨어 다운로드]]에서 최신 버전의 DabitOne 다운로드
```

> ⚠️ 5번 다운로드 페이지는 유지 결정. 그러나 거기엔 DabitOne 항목이 없음. 추후 다운로드 페이지에 DabitOne 항목 추가가 필요할 수 있으나 이 PR에서는 미처리 (사용자 결정대로).

---

### C-2. `2. 사용자 매뉴얼/2.4. 폰트 설정/2.4.2. 폰트 옵션 설명.md`

**라인 7**:
```diff
-- 08x16, 16x16, 16x32, 32x32는 모듈의 픽셀을 의미 (DBPS에서는 삭제)
+- 08x16, 16x16, 16x32, 32x32는 모듈의 픽셀을 의미 (DabitOne에서는 삭제)
```

**라인 20**:
```diff
-- 참고: DBPS에서 옵션 명칭(DABIT Protocol Simulator에서 옵션 명칭)
+- 참고: DabitOne에서 옵션 명칭
```

**라인 32**:
```diff
-#### DBPS에서 테스트시 문자인코딩/폰트 선택
+#### DabitOne에서 테스트시 문자인코딩/폰트 선택
```

> ⚠️ 라인 7의 "DBPS에서는 삭제" 의미가 "DBPS 표시에서는 항목이 삭제됨"이라면, DabitOne 표시에서도 동일한지 확인 필요. **확인 필요**.

---

### C-3. `2. 사용자 매뉴얼/2.4. 폰트 설정/2.4.3. 사용자 폰트 입력 방법.md`

**라인 7**:
```diff
-- DBPS 또는 다빛채의 Sys > Font 폴더에서 USER로 시작하는 사용자폰트를 이용하고, 필요시 fontman.exe 프로그램으로 원하는 문자를 수정/입력 가능
+- DabitOne의 Sys > Font 폴더에서 USER로 시작하는 사용자폰트를 이용하고, 필요시 fontman.exe 프로그램으로 원하는 문자를 수정/입력 가능
```

> ⚠️ "Sys > Font 폴더"는 설치 디렉토리 경로일 가능성이 높음. DabitOne의 실제 설치 디렉토리 구조도 같은가? **확인 필요**.

---

### C-4. `2. 사용자 매뉴얼/2.7. 컨트롤러에 이미지 전송(다빛채)/2.7.1. 이미지 생성.md`

> 폴더 이름은 유지 결정 (4-4). 본문도 폴더 이름과 일관성 위해 (다빛채) 부제 유지가 자연스러우나, 사용자 의도가 1~3 범위 모두 DabitOne 안내라면 변경. **확인 필요**.

**라인 2 (제안 변경)**:
```diff
-> [!abstract] 이미지 생성 (다빛채)
+> [!abstract] 이미지 생성 (DabitOne)
```

**라인 23-24**:
```diff
-- 저장한 .bmp /.jpg 파일을 다빛채에 적용시키기 위해서는 ...
-- (참고) .gif (애니메이션 이미지)를 다빛채에 적용시키기 위해서는 ...
+- 저장한 .bmp /.jpg 파일을 DabitOne에 적용시키기 위해서는 ...
+- (참고) .gif (애니메이션 이미지)를 DabitOne에 적용시키기 위해서는 ...
```

**라인 26 (외부 PDF — 유지)**: `다빛채 매뉴얼 > 첨부2` 그대로 유지 (4-6 결정).

---

### C-5. `2. 사용자 매뉴얼/2.7. 컨트롤러에 이미지 전송(다빛채)/2.7.2. 이미지 또는 문자를 dat 또는 ani 파일로 변환.md`

**라인 2**:
```diff
-> [!abstract] 이미지 변환 (다빛채)
+> [!abstract] 이미지 변환 (DabitOne)
```

**라인 27**:
```diff
-- ani 파일의 프레임이 많아 용량이 커진다면 압축이 적용된 daz, anz 파일로 저장할 것을 권장 (다빛채 V2.4.8 이상)
+- ani 파일의 프레임이 많아 용량이 커진다면 압축이 적용된 daz, anz 파일로 저장할 것을 권장 (DabitOne 이상)
```

> ⚠️ "다빛채 V2.4.8 이상" 표현을 "DabitOne 이상"으로 단순화? 아니면 DabitOne의 특정 버전을 명시? **확인 필요**.

**라인 29 (외부 PDF — 유지)**: 그대로

---

### C-6. `2. 사용자 매뉴얼/2.7. 컨트롤러에 이미지 전송(다빛채)/2.7.3. 표시목록 만들기.md`

**라인 14 (외부 PDF — 유지)**: 그대로 유지 (4-6 결정).
> 처리 라인 없음. 파일 미수정.

---

### C-7. `2. 사용자 매뉴얼/2.8. 다빛솔루션 ASCII 프로토콜/2.8.1. ASCII 프로토콜 매뉴얼.md`

**라인 156**:
```diff
-    - 다빛채 소프트웨어에서 배경 이미지를 만들어 컨트롤러에 저장한 후, 해당 번호를 호출하여 전광판에 표시 (참고: [[3.3. 배경이미지 제작 및 표출]])
+    - DabitOne 소프트웨어에서 배경 이미지를 만들어 컨트롤러에 저장한 후, 해당 번호를 호출하여 전광판에 표시 (참고: [[3.3. 배경이미지 제작 및 표출]])
```

---

### C-8. `3. 전문자료/3.15. LED 모듈 잡티 발생시 해결방법.md`

**라인 2**:
```diff
-> [!abstract] 다빛채에서 업로드한 내용 중 LED 모듈 색상이 이상하게 표시될 때 해결 방법
+> [!abstract] DabitOne에서 업로드한 내용 중 LED 모듈 색상이 이상하게 표시될 때 해결 방법
```

**라인 7**:
```diff
-PC에서 다빛채로 LED 모듈에 문구를 업로드 했을 때 **색상이 의도와 다르게 표시**되는 경우 (예: 노란색이어야 하는데 빨강/초록색 점이 섞여서 나옴)
+PC에서 DabitOne으로 LED 모듈에 문구를 업로드 했을 때 **색상이 의도와 다르게 표시**되는 경우 (예: 노란색이어야 하는데 빨강/초록색 점이 섞여서 나옴)
```

**라인 28**:
```diff
-1. **다빛채 프로그램**을 다시 실행
+1. **DabitOne 프로그램**을 다시 실행
```

**라인 35**:
```diff
-Windows의 화면 효과(글자를 부드럽게 보이게 하는 기능)가 켜져 있으면, 다빛채에서 만드는 표시파일에 불필요한 점들이 포함됩니다.
+Windows의 화면 효과(글자를 부드럽게 보이게 하는 기능)가 켜져 있으면, DabitOne에서 만드는 표시파일에 불필요한 점들이 포함됩니다.
```

---

### C-9. `3. 전문자료/3.4. 표시목록(pla파일) 설정 및 표출.md`

**라인 7**:
```diff
-2. [[2.7.2. 이미지 또는 문자를 dat 또는 ani 파일로 변환]]: 다빛채에 이미지를 불러오기 위해 dat 또는 ani 파일로 변환
+2. [[2.7.2. 이미지 또는 문자를 dat 또는 ani 파일로 변환]]: DabitOne에 이미지를 불러오기 위해 dat 또는 ani 파일로 변환
```

---

## D. 처리 안 함 (사용자 결정대로 유지)

| 파일 | 라인 | 사유 |
| --- | --- | --- |
| `2.1.5. BLE 통신 설정.md` | 34, 36, 40, 50 | 모바일 어플(다빛채 어플 / DabitChe_App) — 유지 |
| `2.4.4. 세로형 폰트 설정 방법.md` | 47 | 모바일 어플 (다빛채 어플) — 유지 |
| `2.4.5. 16x16 폰트(영문,숫자) 설정 방법.md` | 47 | 모바일 어플 — 유지 |
| `2.7.1. 이미지 생성.md` | 26 | 외부 PDF 링크(`DabitChe_UserManual_v9.0.pdf`) — 유지 |
| `2.7.2. 이미지 또는 문자를 dat 또는 ani 파일로 변환.md` | 29 | 외부 PDF 링크 — 유지 |
| `2.7.3. 표시목록 만들기.md` | 14 | 외부 PDF 링크 — 유지 |
| `3.9. 접점신호 설정별 리턴값.md` | 73 | 외부 PDF 링크 — 유지 |
| `2.10. DBPS(...)/` 폴더 전체 | - | dbPS 자체 매뉴얼 — 유지 (4-1) |
| `5. 소프트웨어 다운로드/` 전체 | - | 4-2~4-7 결정대로 유지 |
| `6. 다빛솔루션 소개/` 전체 | - | 역사 기록 — 유지 |
| `2.7. 컨트롤러에 이미지 전송(다빛채)/` 폴더 이름 | - | 폴더 이름은 유지 (4-4) |

---

## E. 의사결정 필요 항목 (Phase 3 진입 전)

| # | 항목 | 결정 필요 |
| - | --- | --- |
| E-1 | A-3 (2.6.2) 본문 `모듈배열방법: 가로형(default)` | UI 라벨이 "배열"인데 본문에선 "모듈배열방법"으로 설명. 본문 단어를 UI 라벨에 맞춰 "배열"로 통일할지, 매뉴얼 설명용 "모듈배열방법"으로 유지할지 |
| E-2 | A-4 (3.21) `정보 > 버전 읽기` 메뉴 | DabitOne에 별도 "정보" 메뉴 없음. `고급 > 펌웨어 업데이트 > 펌웨어 읽기`로 통합 매핑 — 맞나? |
| E-3 | A-5 (3.22) BLE 다이얼로그의 `검색`/`전송`/`저장`/`닫기` 흐름 | 변경 후 흐름이 원본 의도(이름·비밀번호 변경 → 적용)에 맞나? "변경할 이름과 비밀번호 입력 > 전송" 후 별도로 "저장" 클릭이 맞는가? — DabitOne BLE 다이얼로그의 실제 워크플로 검증 필요 |
| E-4 | B-1-1 (2.1.3) RS485 Address 위치 | 구 DBPS는 `통신 설정 > Serial > RS-485 Address`였으나, DabitOne은 통신 설정에는 없고 `고급 > 보드기능설정 > RS485 Address`에 있음. 매뉴얼 본문도 그 위치로 안내하는 게 맞나? |
| E-5 | B-1-9 (3.24) "폰트 두께 계수" → "폰트 가중치" 제목/본문 변경 여부 | DabitOne 다이얼로그명은 "폰트 가중치"이지만 매뉴얼 제목과 본문에선 두께 계수를 그대로 두고 메뉴만 "폰트 가중치"로 안내할지, 매뉴얼 제목까지 "폰트 가중치"로 변경할지 |
| E-6 | B-1-10 (3.3) 단독 1번 이미지 전송 | 구 다빛채는 단독 1번 이미지 전송과 스케줄이 분리. DabitOne은 `배경 스케줄`만 보임. 단독 1번 이미지 전송이 어떻게 처리되는지 추가 확인 필요 |
| E-7 | B-2-1 (2.9.1) 펌웨어 업로드 본문 전체 | line 36의 `### 다빛채` 헤더만 변경하고 그 아래 본문은 그대로 두는지, 본문도 DabitOne 메뉴로 변경하는지 — 파일 전체 보고 결정 |
| E-8 | B-2-2 (3.10) 조도센서 다빛채 설정 본문 | line 13 헤더만 변경하고 그 아래 본문은 그대로 두는지, DabitOne `설정 > 고급설정 > 휘도 고급설정 > CDS연동`으로 안내할지 |
| E-9 | C-2 (2.4.2 line 7) "DBPS에서는 삭제" | DBPS 표시에서 항목이 삭제됨이란 의미라면, DabitOne 표시에서도 동일하게 삭제됨이 맞나? UI 검증 필요 |
| E-10 | C-3 (2.4.3) `Sys > Font 폴더` | DabitOne 설치 디렉토리에도 같은 구조? `D:\Gitea\dabitche\DabitChe.Desktop\bin\Debug\net9.0-windows\Data\Font\` 와 `Font\` 두 폴더 발견됨. 정확한 폴더 경로 확정 필요 |
| E-11 | C-4 (2.7.1 line 2) 폴더 이름은 `(다빛채)` 유지인데 본문 `(다빛채)` 부제는 변경? | 폴더 이름이 (다빛채)인데 본문 부제만 (DabitOne)으로 바꾸면 모순. 부제도 유지 vs 변경 결정 |
| E-12 | C-5 (2.7.2 line 27) `다빛채 V2.4.8 이상` 버전 표현 | DabitOne의 특정 버전 표시할지, "DabitOne 이상"으로 단순화할지, 또는 라인 자체를 삭제할지 |

---

## F. Phase 3 실행 계획 요약

위 모호 항목 답변 받은 뒤:
1. 한 문서당 1커밋 (커밋 메시지: `docs: migrate <파일경로> to DabitOne`)
2. 30개 파일 수정 (A 5 + B-1 10 + B-2 5 + C 10)
3. Phase 4: `DBPS|dbps|다빛채|DabitChe` 잔여 grep, 의도적 잔여(폴더명/모바일 어플/연혁/외부 PDF 등)는 화이트리스트로 보고
